// @flow
import { devFreeze } from "@rpldy/shared";

export const DEFAULT_OPTIONS = devFreeze({

    featureDetection: false,
    version: "1.0.0",
    // extensions: null,
    deferLength: false,
});
