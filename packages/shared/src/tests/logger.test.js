describe("logger tests", () => {

	it("should return true for debug enabled", () => {


	});

	it("should write to console when debug enabled", () => {
		
	});

	it("should not write to console when debug disabled", () => {

	});

});